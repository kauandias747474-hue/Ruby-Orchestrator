source 'https://rubygems.org'

# Gem para automação de tarefas
gem 'rake'

# Grupo de ferramentas para desenvolvimento e testes
group :development, :test do
  gem 'rspec'       # Necessário para rodar os testes na pasta /spec
  gem 'pry'         # Permite pausar o código para depuração (debug)
end
